#include <stdio.h>
#include <stdlib.h>

// Resolução do primeiro exercício da Prova 1 de ESTRUTURA DE DADOS 1

// Declare um vetor de inteiros de tamanho 10 e inicialize todos

int main(){
    int v[10] = {0}; // Inicializa todos os elementos com 0
    int i;

    // como inicializar para contar de 1 a 10?

    for ( i = 0; i < 10; i++){
        //v[i] = i + 1; // Atribui valores de 1 a 10
        printf("%d\n", v[i]);
    }
    



    return 0;
}
